import { NodeActivity, ITransform } from '../core';

/**
 * transform activity.
 *
 * @export
 * @abstract
 * @class TransfrmActivity
 * @extends {NodeActivity<ITransform>}
 */
export abstract class TransformActivity extends NodeActivity<ITransform> {

}
